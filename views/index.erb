<div class='content>'
<h2>Home Page</h2>
<h3>All videos</h3>

 <% @videos.each do |video| %>
        <tr>
          <td>
            <a href="/videos/<%= video['id'] %>">
              <%= [video['artist'], video['title']].join(" - ") %>
            </a>
          </td>
        </tr>
      <% end %>
</div>
      